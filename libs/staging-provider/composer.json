{
    "name": "keboola/workspace-provider",
    "license": "MIT",
    "type": "library",
    "description": "",
    "keywords": ["keboola", "workspace-provider"],
    "authors": [
        {
            "name": "Keboola",
            "email": "devel@keboola.com"
        }
    ],
    "config": {
        "sort-packages": true
    },
    "autoload": {
        "psr-4": {
            "Keboola\\WorkspaceProvider\\": "src/"
        }
    },
    "autoload-dev": {
        "psr-4": {
            "Keboola\\WorkspaceProvider\\Tests\\": "tests/"
        }
    },
    "require": {
        "php": ">=5.6",
        "keboola/input-mapping": "^12.5",
        "keboola/output-mapping": "^12.5|^13.2|^14.3",
        "keboola/storage-api-client": "^11.2|^12.0"
    },
    "require-dev": {
        "phpunit/phpunit": "^5.7",
        "squizlabs/php_codesniffer": "^3.5"
    },
    "scripts": {
        "phpcs": "phpcs --standard=psr2 --ignore=vendor -n src/",
        "phpunit": "phpunit --coverage-clover build/logs/clover.xml --coverage-xml=build/logs/coverage-xml --log-junit=build/logs/phpunit.junit.xml",
        "build": [
            "@phpcs",
            "@phpunit"
        ],
        "ci": [
            "@composer validate --no-check-all --strict",
            "@build"
        ]
    }
}

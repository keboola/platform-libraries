parameters:
    app_name: api-bundle-test

framework:
    test: true
    secret: test
    http_method_override: false

monolog:
    handlers:
        main:
            type: stream
            path: php://stdout
            level: debug
